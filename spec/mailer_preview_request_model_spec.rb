require 'spec_helper'

describe MailerPreviewRequestModel do
  it 'has a version number' do
    expect(MailerPreviewRequestModel::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
